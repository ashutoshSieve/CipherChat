<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cipher Chat</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <!-- Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <style>
        body {
            font-family: Arial, sans-serif;
            height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .navbar {
            background-color: transparent;
        }

        .navbar-brand {
            font-size: 24px;
            font-weight: bold;
        }

        h3 {
            text-align: center;
            margin-top: 20px;
        }

        form.search-form {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        form.search-form input[type="text"] {
            width: 30%;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            margin-right: 10px;
        }

        form.search-form button {
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
        }

        form.search-form button:hover {
            background-color: #0056b3;
        }

        #btn {
            display: block;
            margin: 20px auto;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background-color: #007bff;
            color: white;
            font-size: 18px;
            border: none;
            cursor: pointer;
        }

        #btn:hover {
            background-color: #0056b3;
        }

        .cards-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: flex-start;
            margin: 20px;
        }

        .small-card {
            margin: 10px;
            width: 15rem; /* Reduced the width */
            background-color: #f8f9fa;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .small-card .card-body {
            padding: 10px;
        }

        .footer {
            padding: 20px 0;
            position: relative;
            bottom: 0;
            width: 100%;
        }

        .footer a {
            text-decoration: none;
            color: #007bff;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        /* The popup box (hidden by default) */
        .popup {
            display: none; /* Hidden by default */
            position: fixed; /* Stay in place */
            left: 50%;
            top: 50%;
            transform: translate(-50%, -50%);
            border: 2px solid #333;
            padding: 20px;
            background-color: white;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            z-index: 1001; /* Make sure it appears above the overlay */
            border-radius: 8px; /* Rounded corners */
        }

        /* Dark background overlay */
        .overlay {
            display: none;
            position: fixed;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000; /* Make sure it appears below the popup */
        }

        /* Button to close the popup */
        .close-btn {
            cursor: pointer;
            background-color: transparent;
            border: none;
            position: absolute;
            right: 10px;
            top: 10px;
            font-size: 20px; /* Increase font size for better visibility */
            color: #f00; /* Red color for the cross */
        }

        /* Popup title */
        .popup h4 {
            margin-bottom: 10px; /* Spacing below title */
            position: relative; /* Needed for positioning the close button */
        }

        /* Smaller Create button */
        .create-btn {
            padding: 5px 10px; /* Smaller padding */
        }
    </style>
</head>

<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">CipherChat</a>
            <a class="nav-link" href="/">Home</a>
        </div>
    </nav>

    <!-- Heading -->
    <h3>Community</h3>

    <!-- Search Bar below heading, centered and small input -->
    <form action="/search" method="post" class="search-form">
        <input type="text" name="txt" placeholder="Search community...">
        <button type="submit">Search</button>
    </form>

    <!-- Circular Create Button -->
    <button type="button" id="btn">Create</button>

    <!-- Cards Container -->
    <div class="cards-container">
        <!-- Dynamically added cards -->
        <% if (Array.isArray(data)) { %>
            <% data.forEach((ele) => { %>
                <div class="small-card">
                    <div class="card-body">
                        <form action="/login" method="post">
                            <p><%= ele %></p>
                            <input type="text" name="pass" placeholder="Enter password">
                            <input type="hidden" name="type" value="comm">
                            <input type="hidden" name="name" value="<%=ele%>">
                            <button type="submit" class="btn btn-primary mt-2">Enter</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        <% } %>
    </div>

    <!-- Popup and Overlay -->
    <div class="overlay" id="overlay"></div>
    <div class="popup" id="popup">
        <h4>Create Community</h4>
        <button class="close-btn" id="closeBtn">&times;</button> <!-- Cross icon -->
        <form action="/com/signup" method="post">
            <input type="text" placeholder="Community Name" required name="name">
            <input type="pass" placeholder="pass" required name="pass">
            <button type="submit" class="btn btn-primary create-btn mt-2">Create</button> <!-- Smaller button -->
        </form>
    </div>

    <!-- Footer -->
    <footer class="footer text-center text-lg-start mt-5">
        <div class="text-center p-3">
            <a href="https://www.linkedin.com/in/your-profile/" target="_blank" class="text me-3">
                <i class="fab fa-linkedin"></i> LinkedIn
            </a>
            Made by Ashutosh Gupta
        </div>
    </footer>

    <script src="/docs/5.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <script>
        // Get the popup, overlay, and the button elements
        const btn = document.getElementById("btn");
        const popup = document.getElementById("popup");
        const overlay = document.getElementById("overlay");
        const closeBtn = document.getElementById("closeBtn");

        // When the button is clicked, show the popup and overlay
        btn.addEventListener("click", () => {
            popup.style.display = "block";
            overlay.style.display = "block";
        });

        // Close the popup when the close button (cross) is clicked
        closeBtn.addEventListener("click", () => {
            popup.style.display = "none";
            overlay.style.display = "none";
        });

        // Close the popup if the overlay is clicked
        overlay.addEventListener("click", () => {
            popup.style.display = "none";
            overlay.style.display = "none";
        });
    </script>
</body>

</html>
